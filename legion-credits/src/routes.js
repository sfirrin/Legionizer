import React from 'react'
import ReactDOM from 'react-dom'

import App from './App'
import { HashRouter, Route, browserHistory } from 'react-router'

const Routes = render(props) => (

)

export default Routes